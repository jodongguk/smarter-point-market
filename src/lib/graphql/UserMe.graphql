query UserMe {
    userMe {
        id
        name
        phoneNumber
        birthDate
        credit {
            balance
        }
        authorities {
            authorityName
        }
    }
}